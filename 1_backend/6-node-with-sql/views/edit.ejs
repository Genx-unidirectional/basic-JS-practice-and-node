<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            margin: 0px;
            padding: 0px;
            box-sizing: border-box;
        }

        body {
            min-height: 100vh;
            padding: 1rem;
        }

        form {
            background-color: aqua;
            padding: 1rem;
            display: flex;
            flex-flow: column;
            gap: 1rem;
            border-radius: 0.5rem;
        }

        label {
            font-size: 1.5rem;
            color: white;
        }

        input {
            color: black;
            padding: 1rem;
            font-size: 1.5rem;
            width: 100%;
            border-radius: 0.5rem;
        }

        button {
            font-size: 1rem;
            padding: 0.5em;
            width: 20%;
            align-self: flex-end;
            background-color: white;
            color: brown;
            border: 2px solid black;
            border-radius: 0.5rem;
        }
    </style>
</head>

<body>
    <form method="post" action="/users/<%=user.id  %>?_method=PATCH">
        <label for="username">Username:</label>
        <input type="text" name="formUsername" value="<%= user.username %>" id="username">


        <label for="password">Password:</label>
        <input type="password" name="formPassword" id="password">
        <button type="submit">Submit</button>
    </form>

</body>

</html>